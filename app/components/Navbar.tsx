// Navbar.tsx
import React from 'react';

const Navbar: React.FC = () => {
    return (
        <nav>
            {/* Your Navbar content */}
        </nav>
    );
};

export default Navbar;
